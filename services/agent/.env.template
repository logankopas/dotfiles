# Agent container environment
# Secrets are pulled from Proton Pass vault "homelab" during installation
# (homelab.sh: each ${NAME} -> item titled NAME, value from its "password" field).
# The generated .env is written to /var/selfhosted/agent/.env and is gitignored.

# Qwen Code API key — Proton Pass item "QWEN_API_KEY", field "password"
BAILIAN_TOKEN_PLAN_API_KEY=${QWEN_API_KEY}
